# Data

Local runtime data for the live-business scaffold.

## Files
- `leads.json` — structured lead submissions
- `leads.csv` — spreadsheet-friendly export of the same submissions

These are created automatically by `site/server.js` when needed.
